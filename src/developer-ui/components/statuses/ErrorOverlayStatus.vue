<script setup lang="ts">
import { ref } from "vue"

import ErrorOverlayIcon from "@/developer-ui/components/icons/ErrorOverlayIcon.vue"

import { viteConfigs } from "@/developer-ui/ui-config/UIConfigs"

let iconType = ref(viteConfigs.isErrorOverlayEnebled ? "success" : "error")
let tooltipText = ref(viteConfigs.isErrorOverlayEnebled ? "enabled" : "disabled")
</script>

<template>
  <NTooltip trigger="hover" :delay="400">
    <template #trigger>
      <Row no-padding m-0>
        <NButton disabled quaternary circle :type="iconType">
          <NIcon size="2rem">
            <ErrorOverlayIcon />
          </NIcon>
        </NButton>
      </Row>
    </template>
    Error widget {{ tooltipText }}
  </NTooltip>
</template>

<style lang="scss" scoped></style>
@/ui-config/UIConfigs
