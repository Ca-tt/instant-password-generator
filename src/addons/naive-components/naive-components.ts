/* Naive UI */
import {
    create,
    NButton,
    NIcon,
    NTooltip,
    NMessageProvider,
} from 'naive-ui';


export let naive = create({
    components: [
        NButton,
        NTooltip,
        NIcon,
        NMessageProvider,
    ]
})